import EventEmitter from 'events';

export default class HttpMessage extends EventEmitter {

    constructor () {

        super();

        initProps(this);

    }

}

function initProps (httpMessage) {

    

}
